{
    "properties": {
      "dialogs":         { "type": "string", "analyzer": "{{ item.analyzer }}" }
{% for n in range(3) %}
     ,"agent{{ n }}":    { "type": "string", "analyzer": "{{ item.analyzer }}" }
     ,"customer{{ n }}": { "type": "string", "analyzer": "{{ item.analyzer }}"}
{% endfor %}
    }
}